# Weather App

## Project Structure
- `weather_app/`: Root directory.
- `core/`: Contains `engine.py` for data processing logic.
- `assets/`: Contains `styles.css` (dark theme) and `config.json` for settings.
- `main.py`: Main entry point that integrates the engine.